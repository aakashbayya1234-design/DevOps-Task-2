services:

  web:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: devops-web

    ports:
      - "5000:5000"

    environment:
      APP_ENV: production

    restart: unless-stopped